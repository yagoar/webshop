sudo: true
language: java
jdk:
 - oraclejdk8

cache:
  directories:
    - node_modules

dist: trusty

node_js:
  - '5.6.0'

branches:
  only:
  - master

before_script:
 - npm install -g @angular/cli
 - npm install

script: ng build

deploy:
 provider: heroku
 api-key:
  secure: c8e184c6-1cae-4029-bdad-27b1a4c566ea
 app: studienarbeit-webshop